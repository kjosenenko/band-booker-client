const initialState = {
    slots: [],
    loading: true
}

const scheduleReducer = (state, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default scheduleReducer